# .github/workflows/cd.yml
# main 브랜치 push → Docker 이미지 빌드 → 서버 자동 배포

name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:             # 수동 실행 가능

env:
  REGISTRY:   ghcr.io
  IMAGE_NAME: ${{ github.repository }}   # ghcr.io/username/eum

jobs:
  # ── Docker 이미지 빌드 & 푸시 ────────────────────────────
  build-and-push:
    name: Docker 이미지 빌드
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}

    steps:
      - uses: actions/checkout@v4

      - name: Docker Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: GitHub Container Registry 로그인
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 이미지 메타데이터 설정
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=ref,event=branch
            latest

      - name: 빌드 & 푸시
        id: build
        uses: docker/build-push-action@v5
        with:
          context:   .
          push:      true
          tags:      ${{ steps.meta.outputs.tags }}
          labels:    ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to:   type=gha,mode=max
          build-args: |
            NEXTAUTH_URL=${{ secrets.NEXTAUTH_URL }}

  # ── 서버 배포 ────────────────────────────────────────────
  deploy:
    name: 서버 배포
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production      # GitHub 환경 보호 규칙 적용

    steps:
      - name: 서버에 SSH 배포
        uses: appleboy/ssh-action@v1
        with:
          host:     ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key:      ${{ secrets.DEPLOY_SSH_KEY }}
          port:     ${{ secrets.DEPLOY_PORT || 22 }}
          script: |
            set -e
            cd /opt/eum

            echo "🔐 GitHub Container Registry 로그인..."
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "📦 최신 이미지 풀..."
            docker compose pull app

            echo "🗄️  DB 마이그레이션..."
            docker compose run --rm app npx prisma migrate deploy

            echo "🔄 무중단 재시작 (rolling restart)..."
            docker compose up -d --no-deps --force-recreate app

            echo "🧹 이전 이미지 정리..."
            docker image prune -f

            echo "✅ 배포 완료!"
            docker compose ps

      - name: 배포 성공 알림 (Slack)
        if: success()
        uses: slackapi/slack-github-action@v1
        with:
          payload: |
            {
              "text": "✅ *이음* 배포 완료\n커밋: ${{ github.sha }}\n작성자: ${{ github.actor }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        continue-on-error: true   # Slack 미설정 시에도 실패 안 함

      - name: 배포 실패 알림
        if: failure()
        uses: slackapi/slack-github-action@v1
        with:
          payload: |
            {
              "text": "❌ *이음* 배포 실패\n커밋: ${{ github.sha }}\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|로그 보기>"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        continue-on-error: true
